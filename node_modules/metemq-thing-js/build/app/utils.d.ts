/**
 * Generate a random Message-ID
 * (default value of len: 8)
 */
export declare function genMsgId(len?: number): string;
/**
 * Parse comma-separated string, and returns array of values.
 * Accepted types of values are string or number.
 * @param csvString comma-separated string
 * @returns         Array of values
 */
export declare function parseCSV(csvString: string): Array<string | number>;
export declare function parseValue(csvString: any): number | string | Array<string | number>;
/**
 * Make comma-separated string from array.
 * @param values  Array of values
 * @returns       comma-separated string
 */
export declare function mkString(values: any): string;
