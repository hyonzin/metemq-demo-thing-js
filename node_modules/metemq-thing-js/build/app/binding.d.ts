import { Thing } from './Thing';
export declare class Binding {
    private field;
    private setFunction;
    private thing;
    constructor(field: string, setFunction: Function, thing: Thing);
    set(...args: any[]): void;
    private sendBindMsg(value);
}
